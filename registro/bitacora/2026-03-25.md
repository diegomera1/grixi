# Bitácora — 2026-03-25

---

## Sesión 1 — 00:28

### Resumen

Análisis, diseño e implementación del sistema de registro y documentación
del proyecto GRIXI-APP. Se evaluaron 4 estrategias (devlog diario, changelog
semanal, ADRs + changelog, solo commits) y se definió un sistema híbrido
de 4 capas complementarias.

### Acciones

#### [DOCS] Registro — Diseño del sistema de auditoría de desarrollo
- **Archivos:** `registro/README.md`
- **Detalle:** Creada guía completa del sistema de registro que explica:
  bitácora diaria, estado actual, documentación por módulo, y commits.
  Incluye templates, tags, y formato de entradas.
- **Decisión:** Se eligió un enfoque híbrido (archivos + commits) sobre
  solo commits o solo archivos. Los commits capturan el QUÉ/CUÁNDO, los
  archivos capturan el POR QUÉ/ESTADO/LECCIONES. Se descartó el changelog
  separado porque `git log` ya cumple esa función.

#### [CONFIG] Registro — Estado inicial del proyecto
- **Archivos:** `registro/estado-actual.md`
- **Detalle:** Creado snapshot inicial con todos los módulos (11) e
  infraestructura en estado pendiente. Google Workspace CLI marcado
  como configurado.

#### [DOCS] Registro — Documentación técnica por módulo
- **Archivos:** `docs/modulos/.gitkeep`
- **Detalle:** Directorio preparado para documentación incremental de
  cada módulo. Se creará un MD por módulo al iniciar su desarrollo.

#### [CONFIG] GEMINI.md — Reglas de registro y documentación
- **Archivos:** `GEMINI.md`
- **Detalle:** Agregada sección "Registro y Documentación" con 4 reglas:
  bitácora diaria, estado del proyecto, documentación por módulo, y
  formato de commits. Actualizada estructura del proyecto.

---

## Sesión 2 — 00:46

### [DB] Migración 1: Core Multi-Tenant — Tablas + Índices + Constraints
- **Proyecto Supabase:** `zhursgmxnztyepxobvnz`
- **8 tablas creadas:** organizations, platform_admins, profiles, roles, permissions, role_permissions, memberships, invitations
- **15 índices FK** + 2 parciales (active memberships, pending invitations)
- **RLS habilitado** en las 8 tablas
- **Decisiones:** Hard delete (PITR activo), TEXT+CHECK (no ENUM), TIMESTAMPTZ, gen_random_uuid()

### [DB] Migración 2: Funciones + Hook + RLS + Triggers
- **8 funciones:** is_platform_admin, get_user_org_id, get_user_org_ids, has_permission, get_user_role_name, update_updated_at, handle_new_user, custom_access_token_hook
- **14 RLS policies** con `(select ...)` wrapping (5-10x perf boost)
- **6 triggers:** 5 updated_at + 1 on_auth_user_created
- **Grants:** supabase_auth_admin → hook, memberships, roles

### [FIX] Security Advisor — search_path
- **Funciones afectadas:** update_updated_at, custom_access_token_hook
- **Fix:** `ALTER FUNCTION ... SET search_path = ''`
- **Resultado:** 0 lints de seguridad

### [DOCS] TypeScript Types
- **Archivo:** `docs/database.types.ts`
- **Contenido:** Tipos auto-generados para 8 tablas + 6 funciones

---

## Sesión 3 — 23:05

### [CONFIG] Auth Hook — Activación del Custom Access Token Hook
- **Endpoint:** `PATCH /v1/projects/zhursgmxnztyepxobvnz/config/auth`
- **Parámetros:** `hook_custom_access_token_enabled: true`, `hook_custom_access_token_uri: pg-functions://postgres/public/custom_access_token_hook`
- **Activación:** Manual vía Supabase Dashboard → Authentication → Hooks
- **Verificación:** Confirmado via Management API GET → `hook_custom_access_token_enabled: true`
- **Resultado:** El JWT ahora incluye `organization_id` y `user_role` automáticamente
- **Nota:** El PAT del MCP (`sbp_ebf89b4f...`) no tiene scope `auth_config_write`; se identificó el endpoint correcto para futuras automatizaciones
