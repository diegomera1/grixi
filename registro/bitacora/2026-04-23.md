# Bitácora — 2026-04-23

## Sesión 1 — 22:23

### [REFACTOR] Limpieza de `min_plan` en frontend
- `roles.tsx`: Eliminadas 6 refs a `min_plan` — selects, constants (`PLAN_HIERARCHY`, `PLAN_COLORS`, `orgPlanLevel`), UI plan-gating
- `organizations.tsx`: Eliminado filtrado por `min_plan` en creación de orgs — asigna TODOS los permisos según rol
- `plans.tsx`: Eliminado filtrado por `min_plan` en cambio de plan — misma lógica
- **Archivos:** `app/routes/configuracion/roles.tsx`, `app/routes/admin/organizations.tsx`, `app/routes/admin/plans.tsx`

### [DB] Consolidación RLS Fase 2+3
- Drop de 7 policies duplicadas restantes:
  - `organizations`: `platform_admin_manage`, `users_see_own_org`
  - `platform_admins`: `Platform admins see admins`
  - `profiles`: `users_see_own_profile`, `users_update_own_profile`
  - `role_permissions`: `No writes on suspended orgs role_perms`
  - Fase 3: `invitations`: `Admins manage`, `Users see`
  - `memberships`: `Admins manage`, `Users see`
  - `roles`: `Admins manage`, `Users see`
  - `role_permissions`: `Users see`
- **Resultado:** De 31+ policies a 24 (consolidadas sin duplicados)
- Migraciones: `consolidate_remaining_rls_policies`, `consolidate_rls_phase3_remove_old_authenticated`

### [FEAT] Module Gating — Enforcement
- Nuevo archivo: `app/lib/module-guard.server.ts` — `requireModule()` server-side
- Nuevo componente: `app/components/shared/module-guard.tsx` — `<ModuleGuard module="X">`
  - UI premium con icono Lock, mensaje descriptivo, auto-redirect 3s
  - Platform admins siempre bypass
- `authenticated.tsx`: Agregado `enabledModules: string[]` a `TenantContext`
- `finanzas.tsx`: Envuelto con `<ModuleGuard module="finanzas">`
- `ai.tsx`: Envuelto con `<ModuleGuard module="ai">`

### [DOCS] Actualización estado-actual.md
- Corregido conteo de migraciones (21 → 24)
- Corregido conteo de policies (31+ → 24)
- Eliminada ref a `min_plan` en descripción de `permissions`
- RBAC ahora dice "puro RBAC (sin plan gating)"
- Agregada sección "Arquitectura de Control de Acceso" con Module Gating y Error Boundaries
- Actualizada lista de próximos pasos

### Build
- `pnpm build` ✅ (923ms, 0 errores)

