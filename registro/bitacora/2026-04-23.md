# Bitácora — 2026-04-23

## Sesión 1 — 22:23

### [REFACTOR] Limpieza de `min_plan` en frontend
- `roles.tsx`: Eliminadas 6 refs a `min_plan` — selects, constants (`PLAN_HIERARCHY`, `PLAN_COLORS`, `orgPlanLevel`), UI plan-gating
- `organizations.tsx`: Eliminado filtrado por `min_plan` en creación de orgs — asigna TODOS los permisos según rol
- `plans.tsx`: Eliminado filtrado por `min_plan` en cambio de plan — misma lógica
- **Archivos:** `app/routes/configuracion/roles.tsx`, `app/routes/admin/organizations.tsx`, `app/routes/admin/plans.tsx`

### [DB] Consolidación RLS Fase 2+3
- Drop de 7 policies duplicadas restantes:
  - `organizations`: `platform_admin_manage`, `users_see_own_org`
  - `platform_admins`: `Platform admins see admins`
  - `profiles`: `users_see_own_profile`, `users_update_own_profile`
  - `role_permissions`: `No writes on suspended orgs role_perms`
  - Fase 3: `invitations`: `Admins manage`, `Users see`
  - `memberships`: `Admins manage`, `Users see`
  - `roles`: `Admins manage`, `Users see`
  - `role_permissions`: `Users see`
- **Resultado:** De 31+ policies a 24 (consolidadas sin duplicados)
- Migraciones: `consolidate_remaining_rls_policies`, `consolidate_rls_phase3_remove_old_authenticated`

### [FEAT] Module Gating — Enforcement
- Nuevo archivo: `app/lib/module-guard.server.ts` — `requireModule()` server-side
- Nuevo componente: `app/components/shared/module-guard.tsx` — `<ModuleGuard module="X">`
  - UI premium con icono Lock, mensaje descriptivo, auto-redirect 3s
  - Platform admins siempre bypass
- `authenticated.tsx`: Agregado `enabledModules: string[]` a `TenantContext`
- `finanzas.tsx`: Envuelto con `<ModuleGuard module="finanzas">`
- `ai.tsx`: Envuelto con `<ModuleGuard module="ai">`

### [DOCS] Actualización estado-actual.md
- Corregido conteo de migraciones (21 → 24)
- Corregido conteo de policies (31+ → 24)
- Eliminada ref a `min_plan` en descripción de `permissions`
- RBAC ahora dice "puro RBAC (sin plan gating)"
- Agregada sección "Arquitectura de Control de Acceso" con Module Gating y Error Boundaries
- Actualizada lista de próximos pasos

### Build & Deploy
- `pnpm build` ✅ (923ms, 0 errores)
- `git commit` → `534350e` (10 archivos, +235 -79)
- `git push origin main` ✅
- `npx wrangler deploy` ✅ → Version `e5d132af`
  - 41 assets subidos, Worker Startup: 17ms
  - Bindings: KV_CACHE, ASSETS_BUCKET (R2), ADMIN_RATE_LIMITER
  - Rutas: `*.grixi.ai/*`, `grixi.ai` (custom domain)

### Verificación Producción
- `curl https://grixi.ai/` → HTTP 200 (1.0s, 11KB)
- Headers de seguridad: HSTS ✅, CSP ✅, X-Frame-Options DENY ✅, X-Content-Type-Options nosniff ✅
- Supabase Security Advisors: 3 warnings (intencionales — service role policies)

---

## Sesión 2 — 23:33

### [CONFIG] Diagnóstico OAuth `403: org_internal`
- **Problema:** Usuarios invitados no podían iniciar sesión — Google rechazaba con `Error 403: org_internal`
- **Causa raíz:** El Client ID configurado en Supabase pertenece a un proyecto GCP con OAuth Consent Screen en modo **"Internal"** (solo usuarios del mismo dominio)
- **Proyecto GCP afectado:** Project Number `712040016515` — NO es `grixi-workspace` (que es `225748449252`)
- `grixi-workspace` solo tenía clientes para GWS CLI, no para Supabase Auth

### [CONFIG] Cambio OAuth Consent Screen → External
- Navegado a Google Cloud Console → Auth Platform → Público
- Cambiado Tipo de usuario de **Interno → Externo**
- Estado de publicación: **En producción** (cualquier cuenta Google puede autenticarse)
- Proyecto: `grixi-workspace` (`225748449252`)

### [CONFIG] Nuevo OAuth Client ID creado en `grixi-workspace`
- Tipo: Aplicación web
- Nombre: `GRIXI Supabase Auth`
- Redirect URI: `https://zhursgmxnztyepxobvnz.supabase.co/auth/v1/callback`
- **Client ID:** `[REDACTED — ver Supabase Dashboard]`
- **Client Secret:** `[REDACTED — ver Supabase Dashboard]`

---

## 🔴 Pendientes para mañana

### 1. Actualizar Supabase Google Provider (CRÍTICO)
- Ir a [Supabase → Auth → Providers → Google](https://supabase.com/dashboard/project/zhursgmxnztyepxobvnz/auth/providers)
- Reemplazar Client ID y Client Secret con los nuevos de `grixi-workspace`
- Esto resuelve el error `403: org_internal` para usuarios externos

### 2. Leaked Password Protection
- Activar en Supabase Dashboard → Auth → Settings
- Es solo un toggle, no requiere código

### 3. Staging Environment
- Configurar `wrangler env` para preview URLs

