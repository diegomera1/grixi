# Bitácora — 2026-04-01

## Sesión 1 — 20:40

### [CONFIG] Verificación de entorno
- Wrangler autenticado con `diegomera86@gmail.com`, cuenta GRIXI
- Account ID: `9f03d559ba7dab48269e40938b495350`

### [FEAT] Plan de Quick Wins — Sistema Base
- 27 mejoras funcionales aprobadas en 7 fases
- Estimado total: ~8-10 horas
- Fases: Infra → UX → Animations → Dashboard → Profile → Security → Docs

## Sesión 2 — 20:50

### [FEAT] Fase 2: UX & Observabilidad
- **`app/components/shared/toast.tsx`**: Sistema de notificaciones toast con feedback háptico, auto-dismiss, stack de 3 y animaciones fluidas
- **`app/components/shared/breadcrumbs.tsx`**: Breadcrumbs automáticos basados en `handle.breadcrumb` de cada ruta
- **`app/components/shared/page-skeleton.tsx`**: Skeletons de carga con 5 variantes (dashboard, table, settings, detail, chat)
- **`app/routes/authenticated.tsx`**: Integrado ToastProvider + Breadcrumbs en layout principal
- **`app/routes/dashboard.tsx`**: KPIs avanzados (notificaciones no leídas, eventos audit hoy, conversaciones AI), meta titles, handle breadcrumb, HydrateFallback
- **Rutas actualizadas**: `finanzas.tsx`, `ai.tsx`, `notificaciones.tsx`, `configuracion.tsx` con meta+handle

### [FEAT] Fase 3: Perfil de Usuario
- **`app/routes/configuracion/perfil.tsx`**: Perfil completo con:
  - Upload de avatar a R2 (máx 2MB, jpg/png/webp)
  - Edición de nombre
  - Selector de tema (claro/oscuro/sistema) persistido en Supabase
  - Historial de sesiones con browser/OS/IP
- **`app/routes.ts`**: Registrada ruta `perfil` + ruta `offline` para PWA
- **`app/routes/offline.tsx`**: Página offline para PWA con branding GRIXI

### [FEAT] Login History & Auditoría
- **`app/routes/auth.callback.tsx`**: Recording de login history con parseUserAgent (browser/OS/device), auditoría automática `user.login`
- **Migración `create_login_history`**: Nueva tabla con IP, browser, OS, device_type, city, country + índices + RLS

### [CONFIG] CI/CD
- **`.github/workflows/deploy.yml`**: Pipeline automático build → deploy → health check

### [DEPLOY] Producción v20ce2af0
- Build exitoso (939ms)
- Deploy en Cloudflare Workers
- Health check: ✅ todos servicios OK (Supabase, env, R2)
- Login history verificado en DB (Chrome/macOS/desktop)

## Sesión 3 — 22:15

### [FIX] Bug crítico: double-delete en equipo.tsx
- `confirm` no existe server-side → evaluaba `!undefined` = true → DELETE se ejecutaba 2 veces
- Cada eliminación generaba 2 registros de auditoría duplicados
- Archivos: `app/routes/configuracion/equipo.tsx` (lines 124-142)

### [SECURITY] Protección owner auto-eliminación
- Validación: no puedes suspender/eliminar tu propia cuenta
- Validación: no puedes eliminar/suspender al último owner de la organización
- Helper `validateTargetMember()` con queries de conteo de owners activos
- Archivos: `app/routes/configuracion/equipo.tsx`

### [SECURITY] RBAC enforcement en mutations
- `equipo.tsx` actions: verificación de `team.manage` o `team.view` antes de cualquier mutación
- `organizacion.tsx` action: verificación de `org.manage` o `org.settings.update` para logo/settings
- Platform admins bypass automáticamente
- Archivos: `equipo.tsx`, `organizacion.tsx`

### [FIX] Login notifications duplicadas
- Notificación "X inició sesión" estaba en `authenticated.tsx` loader → se ejecutaba en CADA carga de página
- Movida a `auth.callback.tsx` → solo se ejecuta en login real
- Ahora incluye push notification con VAPID keys
- Import `createNotification` removido de `authenticated.tsx`
- Archivos: `authenticated.tsx`, `auth.callback.tsx`

### [PERF] Dashboard resilience con Promise.allSettled
- Las 9 queries paralelas del dashboard usaban `Promise.all` → si 1 fallaba, todo el dashboard crasheaba
- Cambiado a `Promise.allSettled` con fallback a 0/[] por cada query
- Archivos: `app/routes/dashboard.tsx`

### [FIX] listUsers escalabilidad en auditoría
- `listUsers({ perPage: 100 })` no escalaba para >100 usuarios
- Ahora pagina con `perPage: 1000` y fallback "Usuario eliminado" para IDs no encontrados
- Archivos: `app/routes/configuracion/auditoria.tsx`

### [FEAT] Ruta 404 personalizada
- Nueva ruta catch-all `*` → `routes/not-found.tsx`
- Diseño consistente con ErrorBoundary (glow purple, logo, botones Dashboard/Volver)
- Archivos: `app/routes/not-found.tsx`, `app/routes.ts`

### [UI] Loading states en acciones de equipo
- Botones Suspender/Reactivar/Eliminar ahora muestran "…" durante la acción
- `disabled` durante `fetcher.state !== "idle"` previene double-click
- Archivos: `equipo.tsx`

### Build verificado ✅
- `pnpm build` exitoso (1.17s)
- VAPID secrets confirmados via `wrangler secret list`
