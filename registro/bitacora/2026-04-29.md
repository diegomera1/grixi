# Bitácora — 2026-04-29

## Sesión 1 — 00:05

### [FEAT] Feature Flags — Sistema de control por tenant
- **Migración DB:** `create_feature_flags`
  - Tabla `feature_flags` — definición global de flags (key, name, default_enabled, category)
  - Tabla `feature_flag_overrides` — overrides por organización
  - RLS habilitado, solo lectura para authenticated
  - 17 flags seed para módulos y features existentes
  - Categorías: `module`, `beta`, `experimental`, `general`
- **Server:** `lib/feature-flags.server.ts`
  - `getFeatureFlags(env, orgId)` — merge global defaults + org overrides
  - `isFeatureEnabled(flags, key)` — check individual
  - `requireFeatureFlag(flags, key)` — guard con redirect
  - KV cache con 30s TTL
- **Client:** `lib/hooks/use-feature-flag.ts`
  - `useFeatureFlag("key")` → boolean
  - `useFeatureFlags()` → all flags
  - `<FeatureFlagGate flag="key">` — component guard
- **Integración:** Feature flags en TenantContext y authenticated.tsx loader
- **Archivos:** 3 nuevos, 1 modificado

### [FEAT] Error Tracking — Captura automática de errores en prod
- **Migración DB:** `create_error_logs`
  - Tabla `error_logs` — message, stack, source, level, fingerprint
  - Función `generate_error_fingerprint()` para agrupar errores similares
  - 5 índices optimizados (created_at, fingerprint, source, org, unresolved)
- **API:** `routes/api.errors.ts` — POST /api/errors
  - Recibe errores client/server, parsea browser/OS del UA
  - Genera fingerprint via PostgreSQL function
- **Client:** `lib/error-tracking.ts`
  - `initErrorTracking()` — window.onerror + unhandledrejection
  - `reportError()` — para errores capturados en try/catch
  - Debounce 30s por error único, skip en localhost
  - Fire-and-forget (no bloquea UI)
- **Integración:** Auto-init en authenticated.tsx con user/org context
- **Archivos:** 3 nuevos, 1 modificado

### [FEAT] Analytics de Producto — Tracking de uso
- **Migración DB:** `create_analytics_events`
  - Tabla `analytics_events` — event_name, category, properties, session_id
  - Vista materializada `analytics_daily_summary` para agregados diarios
  - 6 índices (created_at, event_name, org, user, session, category)
- **API:** `routes/api.analytics.ts` — POST /api/analytics
  - Acepta batch de hasta 50 eventos
  - Validación de longitud y sanitización
- **Client:** `lib/analytics.ts`
  - `track(eventName, properties)` — evento genérico
  - `trackPageView()` — auto-detect path y módulo
  - `trackFeature(feature, action)` — uso de features
  - `trackAction(action)` — acciones de usuario
  - Batching: acumula 5s o 10 eventos antes de enviar
  - `navigator.sendBeacon` para envío confiable en page close
  - Session ID persistente en sessionStorage
- **Integración:** Auto page_view en authenticated.tsx
- **Archivos:** 3 nuevos, 1 modificado

### Decisiones técnicas:
1. **Sin dependencias externas:** Analytics, Error Tracking y Feature Flags son self-hosted en Supabase. Cero vendor lock-in, cero costos adicionales.
2. **KV Cache para Feature Flags:** 30s TTL evita queries repetitivos sin sacrificar freshness.
3. **Fingerprint para errores:** PostgreSQL SHA-256 hash agrupa errores similares por message+source+route.
4. **sendBeacon para analytics:** Garantiza envío incluso cuando el usuario cierra la pestaña.
5. **Dynamic import:** Error tracking y analytics se cargan via `import()` dinámico para no bloquear el render inicial.

---

## Sesión 2 — 00:20

### [UI] Admin Panel — 3 nuevas páginas de infraestructura

#### Feature Flags Admin (`/admin/feature-flags`)
- **Archivos:** `routes/admin/feature-flags.tsx`
- Header con KPI stats por categoría (module, beta, experimental, general)
- Búsqueda full-text por nombre, key o categoría
- Toggle de default global por flag
- Panel expandible de overrides por organización:
  - Ver overrides existentes con badge ACTIVO/INACTIVO
  - Agregar override: selector de org + botones Activar/Desactivar
  - Eliminar overrides individuales
- Audit logging en cada acción (toggle, override, remove)

#### Error Tracking Admin (`/admin/errors`)
- **Archivos:** `routes/admin/errors.tsx`
- 4 KPI cards: última hora, 24h, client-side, server-side
- Agrupación por fingerprint (errores similares colapsados)
- Filtros: búsqueda, source (client/server/all), mostrar/ocultar resueltos
- Panel expandible con: stack trace, browser, OS, URL, ocurrencias
- Resolución individual o grupal (por fingerprint)
- Badge de severidad por conteo (>10 rojo, >3 amarillo)

#### Analytics Dashboard (`/admin/analytics`)
- **Archivos:** `routes/admin/analytics.tsx`
- 4 KPI cards con delta vs semana anterior: Page Views, Usuarios Únicos, Sesiones, Feature Events
- Gráfico de barras CSS (sin dependencias) — actividad diaria 7 días
- 3 tabs con ranking:
  - Top Páginas: path + count + barra de progreso
  - Top Features: feature name + count + barra
  - Por Organización: org name + count + barra

### [CONFIG] Sidebar + Routes actualizados
- **Archivos:** `components/admin/admin-sidebar.tsx`, `app/routes.ts`
- 3 nuevos NavItems: Feature Flags (Flag), Errores (Bug), Analytics (BarChart3)
- 5 nuevas rutas: 3 admin pages + 2 API endpoints

---

## Sesión 3 — 00:43

### [REFACTOR] Feature Flags → integrados en Org Detail
- **Problema:** Feature flags estaban en una página aislada (`/admin/feature-flags`), sin relación directa con la organización que afectan
- **Solución:** Mover gestión de flags a la tab "Features & Módulos" dentro de `/admin/organizations/:id`
- **Archivos:** `routes/admin/organizations.$id.tsx`
- Cambios:
  - Loader carga `feature_flags` + `feature_flag_overrides` por org
  - Nuevo action `toggle_feature`:
    - Upsert en `feature_flag_overrides` (override por org)
    - Reset a default global (delete override)
    - Sync automático con `org.settings.enabled_modules` para retrocompatibilidad
  - Nueva UI `FeaturesTab`:
    - Flags agrupados por categoría: Módulos, General, Beta, Experimental
    - Toggle inline ON/OFF por cada flag
    - Badge "Override" cuando difiere del default global
    - Botón "Reset" para volver al default
    - Contador de activos/total por categoría
    - KPI resumen: total activos + total overrides
  - Reemplaza la antigua `ModulesTab` (que solo mostraba un grid estático)
- **Audit logging:** Cada toggle registra `feature_flag.toggle` con flag_key, enabled, useDefault

### [FIX] RLS en feature-flags.server.ts
- **Problema:** `fetchFlags()` usaba `SUPABASE_ANON_KEY` — podía fallar con RLS policies que requieren usuario autenticado
- **Solución:** Cambiar a `SUPABASE_SERVICE_ROLE_KEY` (bypass RLS) — los feature flags son datos de sistema, no datos de usuario
- **Archivos:** `lib/feature-flags.server.ts`

### [SECURITY] Redactar OAuth secrets de bitácora
- **Problema:** GitHub Push Protection bloqueó push por OAuth Client ID/Secret en `registro/bitacora/2026-04-23.md`
- **Solución:** Rebase interactivo para redactar secrets de TODO el historial git (0 ocurrencias post-rebase)
- **Archivos:** `registro/bitacora/2026-04-23.md` (3 secrets → `[REDACTED]`)

### Build & Deploy
- `npx react-router build` ✅ (1.10s, 0 errores)
- `npx wrangler deploy` ✅ → Version `798386a8`
- `git push origin main` ✅ (8aaddb1)
- `curl https://grixi.ai/` → HTTP 200 (0.46s)

---

## Sesión 4 — 00:53

### [FEAT] Hub de Administración Completo por Organización (9 Tabs)
- **Archivo:** `routes/admin/organizations.$id.tsx` (+415 líneas, -39 líneas)

#### Loader mejorado:
- Fetch paralelo de `audit_logs` (100 últimos), `user_passkeys`, más `login_history` secuencial (50 últimos)
- Enriquecimiento de miembros con `lastLogin`, `loginCount`, `hasPasskey`
- Stats de seguridad: totalLogins, passkeysEnabled, uniqueIPs

#### Nuevas Tabs:
1. **Overview** — 4 KPI cards (miembros, roles, features, auditoría) + 4 quick stats (invitaciones, dominios, logins, passkeys) + panel info org + timeline actividad reciente
2. **Auditoría** — Tabla filtrable de audit_logs por org con search, filtro por acción, badges de color por categoría, metadata colapsable
3. **Seguridad** — 3 KPI cards (logins, passkeys, IPs únicas) + tabla de historial de logins con usuario, fecha, IP, método

#### Tabs mejoradas:
4. **Members** — Columnas "Último Login" y "Passkey" (badge verde) reemplazan "Nivel" y "Desde"
5. **Settings + Danger Zone** — Sección de configuración general + zona de peligro con botones de Suspender/Archivar/Restaurar org (con confirmación doble)

#### Nuevo action:
- `change_org_status` — Suspender (`suspended`), archivar (`archived`), restaurar (`active`) con audit logging

### Build & Deploy
- `npx react-router build` ✅ (1.06s)
- `npx wrangler deploy` ✅ → Version `3c7b9dcb`
- `git push origin main` ✅ (dd372f7)

