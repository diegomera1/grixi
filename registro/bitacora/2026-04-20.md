# Bitácora — 2026-04-20

## Sesión 1 — 16:00

### [CONFIG] Supabase MCP — Autenticación OAuth Completada
- Flujo OAuth de Supabase completado exitosamente
- Verificación de conectividad: queries directas a la BD funcionando
- Probado: `list_tables`, `execute_sql`, `list_migrations` — todo operativo
- PAT token: `sbp_823a...` almacenado en configuración local

### [DB] Auditoría de Índices
- Análisis de índices en todas las tablas públicas
- Hallazgo crítico: 3 pares de índices duplicados en `audit_logs`:
  - `idx_audit_logs_actor` vs `idx_audit_logs_actor_id` — misma columna
  - `idx_audit_logs_entity_type` vs `idx_audit_logs_entity` — misma columna
  - `idx_audit_logs_org` vs `idx_audit_logs_organization` — misma columna
- Impacto: overhead en writes sin beneficio en reads (duplican trabajo del planner)
- Acción: programado para eliminación en sesión de hardening

### [SECURITY] Revisión de Security Advisors
- Ejecutado `get_advisors` (tipo: security + performance)
- Resultados:
  - 3 warnings de seguridad menores (search_path, RLS always-true en audit_logs/login_history)
  - Leaked password protection: recomendación de habilitar en Auth settings
- Sin vulnerabilidades críticas detectadas
