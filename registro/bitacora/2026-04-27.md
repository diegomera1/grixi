# Bitácora — 2026-04-27

## Sesión 1 — 22:00

### [FIX] Command Palette — Filtrado por Permisos
- **Archivos afectados:**
  - `app/components/shared/command-palette.tsx` — Reestructuración completa
  - `app/routes/authenticated.tsx` — Props de contexto al CommandPalette
- **Detalle:**
  - Cada comando ahora tiene `requiredPermission` y/o `requiredModule` opcionales
  - Auditoría solo visible si usuario tiene `admin.audit`
  - Roles solo visible si usuario tiene `roles.manage`
  - Módulos como Finanzas solo visibles si están en `enabledModules`
  - Platform admins ven todo (bypass de permisos)
  - Fix de navegación: `preventDefault` + `stopPropagation` + fallback `window.location`
  - Props recibidos del `TenantContext`: `permissions`, `enabledModules`, `isPlatformAdmin`

### [FIX] Orb — Branding del Tenant + Notificaciones
- **Archivos afectados:**
  - `app/components/layout/grixi-orb.tsx` — 3 secciones modificadas
- **Detalle:**
  - **Brand en Panel Desktop (línea 794):**
    - Reemplazado "G" + "GRIXI" hardcodeado por logo/inicial del tenant
    - Si `logoUrl` existe → `<img>` con `ring-1 ring-border`
    - Si no → inicial con `brand_color` de settings como fondo
    - Nombre de la organización + "Powered by GRIXI"
  - **Brand en Mobile (línea 1212):**
    - Mismo tratamiento: logo o inicial con color del brand
    - Nombre de org + "Powered by GRIXI"
  - **Notificaciones (línea 792):**
    - Removido `overflow-hidden` del panel para que el dropdown no se corte
    - El dropdown de `w-80` (320px) ahora se renderiza sin clipping fuera del panel de `w-[210px]`
- **Decisiones técnicas:**
  - `overflow-hidden` causaba que el notification dropdown se cortara visualmente
  - Sin `overflow-hidden`, los `border-radius` siguen aplicando al borde/fondo
  - El dropdown tiene su propio `bg-surface`, `border` y `shadow-2xl` — se ve como card flotante independiente

### [CONFIG] Deploy a producción
- Build: `✓ built in 1.28s`
- Deploy Cloudflare Workers: Version `c3d7e5ee` → `grixi.ai`
- Push a main vía `dmera1` → `97e53fe..21ab613`
