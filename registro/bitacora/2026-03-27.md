# Bitácora — 2026-03-27

## Sesión 2 — 18:26

### [FEAT] Admin funcional + Login por tenant + Orb glow
- **Archivos afectados:**
  - `app/routes/admin/organizations.tsx` — Fix insert (plan→settings JSONB)
  - `app/routes/admin/billing.tsx` — ELIMINADO
  - `app/routes.ts` — Ruta billing eliminada
  - `app/components/layout/sidebar.tsx` — Sin billing, orb glow sutil
  - `app/routes/admin/users.tsx` — Estilos unificados
  - `app/routes/admin/audit.tsx` — Estilos unificados
  - `app/routes/login.tsx` — Login personalizado por tenant con logo
  - `public/logos/acme.png` — Logo Acme Corp (AI)
  - `public/logos/nexus.png` — Logo Nexus Technologies (AI)
- **Detalle:**
  - Bug principal: al crear org, `plan` se insertaba como columna, pero solo existe en `settings` JSONB → insert fallaba silenciosamente
  - Billing eliminado del sidebar y rutas
  - Sidebar orb glow cambiado de pulse agresivo a `opacity-10 blur-md` (alineado con demo)
  - Login usa `tenantSlug` del worker para resolver org→logo→nombre→color
  - 3 orgs en DB: GRIXI (enterprise), Acme Corp (professional), Nexus Technologies (starter)
  - Roles creados (owner/admin/member/viewer) para cada org
- **Decisiones técnicas:**
  - Logos servidos desde `/public/logos/` (estáticos) en lugar de Supabase Storage para simplificar
  - Login fallback a GRIXI branding si no hay tenant o org no encontrada
  - `settings` JSONB auto-calcula `max_users` según plan seleccionado

### [DB] Datos demo multi-tenant
- GRIXI org (slug: `grixi`) → logo `/grixi-logo.png`, enterprise
- Acme Corp (slug: `acme`) → logo `/logos/acme.png`, professional
- Nexus Technologies (slug: `nexus`) → logo `/logos/nexus.png`, starter

### [DEPLOY] Deploy a Cloudflare Workers
- Build exitoso, sin errores
- Deploy version: `13efe174`
- Verificado:
  - `grixi.ai` → 200, GRIXI branding ✅
  - `acme.grixi.ai` → 200, Acme Corp branding ✅
  - `nexus.grixi.ai` → 200, Nexus Technologies branding ✅

---

## Sesión 3 — 19:00

### [SECURITY] Aislamiento estricto de admin a grixi.grixi.ai
- **Archivos afectados:**
  - `app/lib/platform-guard.ts` — [NUEVO] Guard centralizado `isPlatformTenant()`
  - `app/routes/admin/index.tsx` — Guard en loader
  - `app/routes/admin/organizations.tsx` — Guard en loader + action
  - `app/routes/admin/organizations.$id.tsx` — Guard en loader + action
  - `app/routes/admin/users.tsx` — Guard en loader + action
  - `app/routes/admin/audit.tsx` — Guard en loader
  - `app/components/layout/sidebar.tsx` — Prop `tenantSlug`, admin solo si `tenantSlug === 'grixi'`
  - `app/routes/authenticated.tsx` — Pasa `tenantSlug` al Sidebar
- **Detalle:**
  - Problema: Desde `nexus.grixi.ai/admin/organizations` se podía acceder y controlar toda la plataforma
  - Solución: 6 capas de seguridad implementadas (ver abajo)
  - Guard en TODOS los `loader()` → redirect a `/dashboard` si no es tenant `grixi`
  - Guard en TODOS los `action()` → 403 Forbidden si no es tenant `grixi`
  - Sidebar oculta sección PLATAFORMA si `tenantSlug !== 'grixi'`
- **Decisiones técnicas:**
  - `tenantSlug` viene del hostname (Cloudflare Worker) → no manipulable por el cliente
  - Guard centralizado en `lib/platform-guard.ts` para consistencia
  - Se valida tanto lectura (loaders) como escritura (actions) para evitar POST directo
- **Capas de seguridad verificadas:**
  1. DNS/Cloudflare — Solo `*.grixi.ai` llega al Worker
  2. Worker Edge — `tenantSlug` del hostname real
  3. Loaders (GET) — `isPlatformTenant()` en 5 loaders admin
  4. Actions (POST) — `isPlatformTenant()` en 3 actions admin
  5. DB `platform_admins` — Verificación de admin en tabla
  6. Sidebar (UI) — Links admin ocultos para no-grixi tenants

### [DEPLOY] Deploy seguridad multi-tenant
- Commit `e8651db` — Guard en loaders
- Commit `fec6aa9` — Guard en actions (corrección crítica)
- Deploy version: `44a052de`
- Verificado: `nexus.grixi.ai/admin/*` → redirect a `/dashboard` ✅
