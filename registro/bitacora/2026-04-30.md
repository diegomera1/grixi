# Bitácora — 2026-04-30

## Sesión 1 — 15:26

### [SECURITY] Hardening del Admin Portal (admin.grixi.ai)

#### [FIX] Bug de Logout no funciona
- **Archivo:** `routes/auth.signout.tsx`
- **Problema:** El sidebar envía `<Form method="post">` pero la ruta solo tenía un `loader` (GET). El POST no era procesado.
- **Solución:** Agregar `action` handler para POST + mantener `loader` como fallback GET
- `scope: "global"` → cerrar todas las sesiones del usuario
- Integrado cleanup de admin session KV en el signout

#### [FEAT] Cookies Session-Only para Admin Portal
- **Archivo:** `lib/supabase/client.server.ts`
- Detección automática de `admin.grixi.ai` via header `host`
- Cookies en admin: sin `maxAge` (session cookie) → cerrar browser = logout
- `SameSite=Strict` + `Secure=true` para anti-CSRF
- Tenants normales mantienen cookies persistentes (Lax)

#### [FEAT] KV Admin Session Guard (30 min inactividad)
- **Archivo:** `lib/platform-rbac/admin-session.server.ts` (NUEVO)
- Timestamp de última actividad en Cloudflare KV (`admin_session:{userId}`)
- Verificación en cada request del admin layout
- TTL de 30 min + 5 min buffer
- Funciones: `requireAdminSession()`, `startAdminSession()`, `clearAdminSession()`

#### [FEAT] Página de Re-Autenticación
- **Archivo:** `routes/admin-reauth.tsx` (NUEVO)
- UI enterprise con branding GRIXI: fondo dark, shield icon, formulario de credenciales
- Verificación de admin en `platform_admins` post-login
- Audit log `admin.session.reauth` en cada re-autenticación exitosa
- Ruta registrada FUERA del admin layout (accesible con sesión expirada)

#### [FEAT] Monitor de Inactividad Client-Side
- **Archivo:** `routes/admin-layout.tsx`
- Hook `useAdminInactivityMonitor()`: detecta mouse, keyboard, scroll, touch
- Warning modal a 25 min con countdown (mm:ss)
- Auto-redirect a `/admin/reauth` a 30 min
- Botón "Continuar Trabajando" resetea todos los timers

#### [FEAT] Audit Logging de Sesiones Admin
- `admin.session.verified` → log al acceder al admin (throttled 10 min via KV)
- `admin.session.reauth` → log al re-autenticarse
- Incluye IP, user-agent, path

### Build & Deploy
- `npx react-router build` ✅ (1.10s)
- `npx wrangler deploy` ✅ → Version `ddc7ca64`
- `git commit` ✅ (484febd)
- `git push` ❌ (conflicto de credenciales diegomera12 vs diegomera1 — pendiente)

### Archivos Afectados
- `routes/auth.signout.tsx` — FIX + refactor
- `lib/supabase/client.server.ts` — admin cookie strategy
- `lib/platform-rbac/admin-session.server.ts` — NUEVO
- `routes/admin-reauth.tsx` — NUEVO
- `routes/admin-layout.tsx` — guard + inactivity monitor
- `routes.ts` — ruta admin/reauth

## Sesión 2 — 16:45

### [SECURITY] Auditoría de Infraestructura Cloudflare + CSP Hardening

#### [FIX] Auth Callback Admin Fast-Path
- **Archivo:** `routes/auth.callback.tsx`
- Platform admins en admin portal skip membership checks → directo a `/admin`
- Non-admins: signout automático + redirect a `/login?error=not_admin`

#### [FIX] Unauthorized Page Admin-Aware
- **Archivo:** `routes/unauthorized.tsx` — reescritura completa
- Botón "Cerrar Sesión" (Form POST), muestra email activo

#### [SECURITY] Deshabilitar workers_dev
- **Archivo:** `wrangler.jsonc` → `workers_dev: false`

#### [SECURITY] Proteger /api/health
- **Archivo:** `routes/api.health.ts` — requiere `X-Health-Secret`

#### [SECURITY] CSP Nonce-Based
- **Archivos:** `workers/app.ts`, `entry.server.tsx`, `root.tsx`
- Nonce criptográfico por request, eliminados `unsafe-inline` y `unsafe-eval` de script-src
- Verificado: nonce coincide entre CSP header y HTML

### Build & Deploy
- Múltiples deploys exitosos. Último: Version `8159f958`
- Commits: `d7ef750`, `6a94747`, `c68daf7`, `bb5432c`
