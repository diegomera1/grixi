# Bitácora — 2026-04-30

## Sesión 1 — 15:26

### [SECURITY] Hardening del Admin Portal (admin.grixi.ai)

#### [FIX] Bug de Logout no funciona
- **Archivo:** `routes/auth.signout.tsx`
- **Problema:** El sidebar envía `<Form method="post">` pero la ruta solo tenía un `loader` (GET). El POST no era procesado.
- **Solución:** Agregar `action` handler para POST + mantener `loader` como fallback GET
- `scope: "global"` → cerrar todas las sesiones del usuario
- Integrado cleanup de admin session KV en el signout

#### [FEAT] Cookies Session-Only para Admin Portal
- **Archivo:** `lib/supabase/client.server.ts`
- Detección automática de `admin.grixi.ai` via header `host`
- Cookies en admin: sin `maxAge` (session cookie) → cerrar browser = logout
- `SameSite=Strict` + `Secure=true` para anti-CSRF
- Tenants normales mantienen cookies persistentes (Lax)

#### [FEAT] KV Admin Session Guard (30 min inactividad)
- **Archivo:** `lib/platform-rbac/admin-session.server.ts` (NUEVO)
- Timestamp de última actividad en Cloudflare KV (`admin_session:{userId}`)
- Verificación en cada request del admin layout
- TTL de 30 min + 5 min buffer
- Funciones: `requireAdminSession()`, `startAdminSession()`, `clearAdminSession()`

#### [FEAT] Página de Re-Autenticación
- **Archivo:** `routes/admin-reauth.tsx` (NUEVO)
- UI enterprise con branding GRIXI: fondo dark, shield icon, formulario de credenciales
- Verificación de admin en `platform_admins` post-login
- Audit log `admin.session.reauth` en cada re-autenticación exitosa
- Ruta registrada FUERA del admin layout (accesible con sesión expirada)

#### [FEAT] Monitor de Inactividad Client-Side
- **Archivo:** `routes/admin-layout.tsx`
- Hook `useAdminInactivityMonitor()`: detecta mouse, keyboard, scroll, touch
- Warning modal a 25 min con countdown (mm:ss)
- Auto-redirect a `/admin/reauth` a 30 min
- Botón "Continuar Trabajando" resetea todos los timers

#### [FEAT] Audit Logging de Sesiones Admin
- `admin.session.verified` → log al acceder al admin (throttled 10 min via KV)
- `admin.session.reauth` → log al re-autenticarse
- Incluye IP, user-agent, path

### Build & Deploy
- `npx react-router build` ✅ (1.10s)
- `npx wrangler deploy` ✅ → Version `ddc7ca64`
- `git commit` ✅ (484febd)
- `git push` ❌ (conflicto de credenciales diegomera12 vs diegomera1 — pendiente)

### Archivos Afectados
- `routes/auth.signout.tsx` — FIX + refactor
- `lib/supabase/client.server.ts` — admin cookie strategy
- `lib/platform-rbac/admin-session.server.ts` — NUEVO
- `routes/admin-reauth.tsx` — NUEVO
- `routes/admin-layout.tsx` — guard + inactivity monitor
- `routes.ts` — ruta admin/reauth

## Sesión 2 — 16:45

### [SECURITY] Auditoría de Infraestructura Cloudflare + CSP Hardening

#### [FIX] Auth Callback Admin Fast-Path
- **Archivo:** `routes/auth.callback.tsx`
- Platform admins en admin portal skip membership checks → directo a `/admin`
- Non-admins: signout automático + redirect a `/login?error=not_admin`

#### [FIX] Unauthorized Page Admin-Aware
- **Archivo:** `routes/unauthorized.tsx` — reescritura completa
- Botón "Cerrar Sesión" (Form POST), muestra email activo

#### [SECURITY] Deshabilitar workers_dev
- **Archivo:** `wrangler.jsonc` → `workers_dev: false`

#### [SECURITY] Proteger /api/health
- **Archivo:** `routes/api.health.ts` — requiere `X-Health-Secret`

#### [SECURITY] CSP Nonce-Based
- **Archivos:** `workers/app.ts`, `entry.server.tsx`, `root.tsx`
- Nonce criptográfico por request, eliminados `unsafe-inline` y `unsafe-eval` de script-src
- Verificado: nonce coincide entre CSP header y HTML

### Build & Deploy
- Múltiples deploys exitosos. Último: Version `8159f958`
- Commits: `d7ef750`, `6a94747`, `c68daf7`, `bb5432c`

## Sesión 3 — 17:11

### [SECURITY] Auditoría Profunda Supabase + DB Hardening

#### [SECURITY] Revocar EXECUTE de anon en funciones SECURITY DEFINER
- **17 funciones** expuestas via `/rest/v1/rpc/` al rol `anon` (sin autenticación)
- Root cause: Supabase default privileges dan `EXECUTE ON ALL FUNCTIONS` a `PUBLIC`
- Fix: `REVOKE EXECUTE ... FROM PUBLIC` + re-grant selectivo a `authenticated`/`service_role`
- Triggers internos (`fn_audit_trigger`, `handle_new_user`, `rls_auto_enable`, `trigger_push_on_notification`) → solo `service_role`
- Funciones RBAC/permisos → solo `authenticated` + `service_role`
- **Verificado:** 0 funciones accesibles por anon (antes: 17)

#### [SECURITY] Fijar search_path en funciones vulnerables
- 5 funciones con `search_path` mutable: inyección potencial
- Fix: `ALTER FUNCTION ... SET search_path = public;`
- Funciones: `trigger_push_on_notification`, `generate_error_fingerprint`, `get_platform_admin_permissions`, `has_platform_permission`, `get_platform_admin_scoped_orgs`

#### [SECURITY] Proteger vista materializada
- `analytics_daily_summary` accesible por `anon` via API
- Fix: `REVOKE SELECT ON analytics_daily_summary FROM anon;`

#### [SECURITY] Rate limiting en reauth
- **Archivo:** `routes/admin-reauth.tsx`
- KV-based: 5 intentos / 15 min por IP
- Limpieza de rate limit en reauth exitoso
- Log de IP en audit_logs

#### [DOCS] Auditoría pendiente
- Leaked Password Protection: activar en Supabase Dashboard → Auth Settings
- Supabase Security Advisor: de 54 issues → 24 issues (17 restantes son intencionales para authenticated)

### Build & Deploy
- Build ✅ (1.62s)
- Deploy ✅ → Version `bc3f5787`
- Commit `d17d415` → push ✅

### Archivos Afectados
- `routes/admin-reauth.tsx` — rate limiting KV
- Migraciones SQL en Supabase: `revoke_anon_security_definer_functions`, `fix_search_path_and_materialized_view`

## Sesión 4 — 22:38

### [SECURITY] Auditoría del Sistema RBAC Admin

#### Análisis Completo
- Verificación code-level de: guard.server.ts, types.ts, hooks.ts, admin-layout.tsx, admin-sidebar.tsx
- Verificación de: users.tsx (loader+action+UI), roles.tsx (CRUD+matrix)
- Verificación DB: platform_roles (6), platform_permissions (25), platform_role_permissions (60)
- Resultado: **3 bugs de seguridad + 1 bug de integridad**

#### [FIX] Bug 1 — `role_id` nullable en `platform_admins`
- **Tabla:** `platform_admins`
- Un admin sin `role_id` crashearía el guard silenciosamente
- Fix: `ALTER COLUMN role_id SET NOT NULL`

#### [FIX] Bug 2 — Guard crash si role join falla
- **Archivo:** `lib/platform-rbac/guard.server.ts`
- Si `platform_roles` join retorna null (rol eliminado, cache corrupto), TypeError
- Fix: null-check defensivo + signout + redirect

#### [FIX] Bug 3 — Separación de duties incompleta
- **Archivo:** `routes/admin/users.tsx`
- `promote` y `update_role` solo verificaban `admin.users.manage`
- Faltaba verificar `admin.users.roles` (permiso específico de asignación de roles)
- Fix: doble verificación de permisos

#### [FIX] Bug 4 — Auto-escalación posible
- **Archivo:** `routes/admin/users.tsx`
- `update_role` no bloqueaba cambiar tu propio rol
- `update_role` no verificaba jerarquía (podías asignar rol >= tu nivel)
- Fix: self-check + hierarchy enforcement

### Build & Deploy
- Build ✅ (1.07s)
- Deploy ✅ → Version `5947a1aa`
- Commit `7c6e847` → push ✅

### Archivos Afectados
- `lib/platform-rbac/guard.server.ts` — null-check defensivo
- `routes/admin/users.tsx` — 3 fixes de seguridad en action
- Migración SQL: `fix_platform_admins_role_not_null`

## Sesión 5 — 00:27

### [SECURITY] Auditoría Profunda: APIs + Funciones DB + RLS

#### Barrido Completo
- Auditados 14 API routes: ai.chat, ai.upload, ai.conversations, analytics, errors, health, push.send, push.subscribe, push.cleanup, finance-analyze, finance-notes, auth.callback, passkey/*
- Auditadas 25 funciones SECURITY DEFINER en PostgreSQL
- Ejecutado Supabase Security Advisor: 24 findings analizados
- Resultado: **7 vulnerabilidades nuevas identificadas y corregidas**

#### [FIX] V1 — `/api/analytics` sin autenticación (🔴 Crítica)
- Endpoint completamente abierto, usaba service_role directo
- `user_id` venía del cliente (spoofable)
- Fix: auth requerida + user_id server-enforced

#### [FIX] V2 — `/api/errors` user_id spoofable (🟡 Media)
- Aceptaba `userId` y `organizationId` del body del request
- Fix: user_id ahora viene de `auth.getUser()`, org_id removido

#### [FIX] V3 — `/api/push/send` bypass RBAC (🟡 Media)
- Solo verificaba existencia en `platform_admins` (cualquier admin podía enviar)
- Fix: requiere `admin.notifications.broadcast` via RBAC guard

#### [FIX] V4 — `/api/push/cleanup` bypass RBAC (🟡 Media)
- Mismo problema que push/send
- Fix: requiere `admin.settings.manage` via RBAC guard

#### [FIX] V5 — `upsert_user_preference` escalación (🔴 Crítica)
- SECURITY DEFINER aceptaba `p_user_id` arbitrario
- Cualquier usuario autenticado podía cambiar preferencias de CUALQUIER usuario
- Fix: validación `auth.uid()` + exception si no coincide

#### [FIX] V6 — `verify_whitelist_access` probe (🟡 Media)
- Permitía a cualquier usuario autenticado probar si un email arbitrario tenía invitaciones/whitelist
- Fix: validación que solo puedes verificar tu propio email

#### [FIX] V7 — RLS gaps en 3 tablas (🟡 Media)
- `analytics_events`, `app_config`, `error_logs`: RLS habilitado pero sin policies
- `analytics_daily_summary`: materialized view accesible por authenticated
- Fix: policies creadas + SELECT revocado

### Build & Deploy
- Build ✅ (1.22s)
- Deploy ✅ → Version `04573fa7`
- Commit `16e7922` → push ✅

### Archivos Afectados
- `routes/api.analytics.ts` — auth + server-enforced user_id
- `routes/api.errors.ts` — server-enforced user_id
- `routes/api.push.send.ts` — RBAC guard completo
- `routes/api.push.cleanup.ts` — RBAC guard completo
- Migraciones SQL: `fix_analytics_matview_authenticated_access`, `fix_upsert_user_preference_self_only`, `fix_verify_whitelist_self_only`

## Sesión 6 — 00:42

### [PERF] Optimización de 14 RLS Policies + Fix Regresión

#### 🔴 Regresión detectada y corregida
- `verify_whitelist_access` — la validación self-only rompía el auth callback
  porque `auth.uid()` es NULL cuando se llama con service_role
- Fix: bypass para service_role, protección mantenida para authenticated users

#### [PERF] auth_rls_initplan — 14 policies optimizadas
- Patrón: `auth.uid()` → `(select auth.uid())` (evaluación 1x por query vs 1x por fila)
- Tablas afectadas: notification_preferences, memberships, ai_conversations,
  ai_messages, user_preferences, login_history, notifications (3 policies),
  push_subscriptions, analytics_events, user_passkeys, user_linked_accounts

#### [SECURITY] audit_logs policy overlap corregido
- "Service role full access" aplicaba a TODOS los roles (anon, authenticated, etc.)
- Corregido: ahora solo aplica a service_role

### Verificación completa
- auth.callback.tsx → flujo de whitelist verificado (service_role bypass funciona)
- R2 SecureR2Client → 7 capas de seguridad verificadas (path traversal, ownership, etc.)
- CSP headers → nonce-based, sin unsafe-eval ✅
- Todos los API routes auditados (14 total): auth correcta en todos
- Commit `9de0c3c` → push ✅

## Sesión 7 — 00:55

### [FEAT] Permisos Granulares por Usuario en Admin

#### [DB] Nueva tabla `platform_admin_permission_overrides`
- Override system: `grant` agrega permiso que el rol no tiene, `deny` quita permiso que el rol sí tiene
- UNIQUE constraint por (admin_id, permission_id)
- RLS: solo service_role
- Audit trail: `granted_by`, `notes`, timestamps

#### [SECURITY] Guard server-side modificado
- `guard.server.ts` ahora aplica: `(permisos_del_rol + grants) - denies`
- Super Admins inmunes a overrides (siempre tienen todo)
- Cache con invalidación automática (3 min TTL)

#### [UI] Modal `UserPermissionsModal`
- Toggle tri-estado por permiso: Default → Grant → Deny → Default
- Agrupado por módulo con iconos (11 módulos)
- Muestra estado efectivo (✅/—), badge "Rol ✓/✗", y botón de override
- Cambios se aplican inmediatamente vía fetcher
- Protecciones: no self-escalation, hierarchy check, super-admin no editable

#### Archivos modificados
- `app/lib/platform-rbac/guard.server.ts` — cargar y aplicar overrides
- `app/routes/admin/users.tsx` — loader, 2 nuevos actions, modal completo
- Migración SQL: `create_platform_admin_permission_overrides`
- Commit `366f475` → push ✅
