# Bitácora — 2026-03-29

## Sesión 1 — 01:00 (completada)
> Migración completa del módulo GRIXI AI a producción

- [FEAT] Migración de 10 componentes de AI (sidebar, chat, canvas, etc.)
- [CONFIG] Integración R2 storage con SecureR2Client
- [FEAT] Upgrade a gemini-3.1-flash-lite-preview
- [CONFIG] Ruta /ai con ClientOnly wrapper
- Deploy exitoso: version cb11dfdf

---

## Sesión 2 — 09:56

### [SECURITY] Fix crítico: Aislamiento de sesiones multi-tenant

**Problema detectado:**
Las sesiones de autenticación se compartían entre TODOS los subdomains de `*.grixi.ai`.
Un usuario logueado en `grixi.grixi.ai` tenía acceso automático a `acme.grixi.ai` y cualquier otro tenant.
Además, los datos del dashboard mostraban información del tenant incorrecto.

**Causa raíz:**
Cookie de Supabase auth configurada con `domain=.grixi.ai`, haciendo que todas las cookies
fueran visibles en todos los subdomains.

**Archivos modificados:**

1. `app/lib/supabase/client.server.ts`
   - [SECURITY] Eliminado `domain=.grixi.ai` de cookies de Supabase
   - Cookies ahora sin `domain=` → aisladas al hostname exacto
   - Agregado `SameSite=Strict`, `Secure`, `HttpOnly`

2. `app/routes/authenticated.tsx`
   - [SECURITY] Cookie `grixi_org` hardened: `SameSite=Strict` + `HttpOnly`
   - Sin `domain=` → aislada por tenant

3. `app/routes/dashboard.tsx`
   - [SECURITY] Dashboard ahora usa `tenantSlug` del Worker context
   - En modo subdomain: valida membership antes de queries
   - Solo carga datos del org que corresponde al subdomain
   - Platform admins pueden acceder sin membership

4. `workers/app.ts`
   - [SECURITY] Middleware `getStaleSessionCleanupHeaders()`
   - Invalida cookies antiguas con `domain=.grixi.ai`
   - Fuerza re-login limpio por cada tenant
   - Transición transparente de cookies compartidas a aisladas

**Decisiones técnicas:**
- Sin `domain=` en cookies → browser default = hostname-only scope
- `SameSite=Strict` en vez de `Lax` → máxima protección
- Cookie cleanup middleware temporal → limpia cookies stale de la configuración anterior
- Platform admins deben loguearse en cada subdomain individualmente

**Deploy:** Version `2a6b9d6a`, commit `fc957ce`

---

## Sesión 3 — 16:00

### [FEAT] RBAC Fase 1: Panel de Configuración por Tenant

**Objetivo:** Implementar sistema completo de RBAC con hooks, guards y panel administrativo `/configuracion` por tenant.

**Foundation RBAC:**

1. `app/lib/hooks/use-permissions.ts` [NEW]
   - [FEAT] Hook `usePermission(key)` — verifica permiso individual
   - [FEAT] Hook `usePermissionAny(keys)` — verifica al menos uno
   - [FEAT] Hook `useTenantContext()` — acceso tipado al contexto del tenant
   - [FEAT] Hook `useRole()` — role + hierarchy_level del usuario
   - Platform admins bypass automático en todos los hooks

2. `app/components/shared/permission-guard.tsx` [NEW]
   - [FEAT] `<PermissionGuard>` — renderizado condicional por permisos
   - Fallback configurable, soporte `any` mode

3. `app/lib/permission-guard.server.ts` [NEW]
   - [FEAT] `requirePermission()` — guard server-side para loaders
   - [FEAT] `requirePermissionAny()` — guard multi-permiso
   - Redirect automático a `/dashboard` si no autorizado

4. `app/components/layout/grixi-orb.tsx`
   - [FEAT] Filtrado de módulos por RBAC (`requiredPermissionAny`)
   - [FEAT] Módulo "Configuración" condicionado a `org.configure` | `members.manage` | `roles.manage`

**Panel /configuracion:**

5. `app/routes/configuracion.tsx` [NEW]
   - [FEAT] Layout con 5 tabs permission-gated
   - Header con info de org (nombre, slug, color)
   - Guard `requirePermissionAny` en loader

6. `app/routes/configuracion/equipo.tsx` [NEW]
   - [FEAT] Stats cards (total, activos, suspendidos, invitaciones)
   - [FEAT] Tabla de miembros con búsqueda
   - [FEAT] Cambio de rol inline (dropdown)
   - [FEAT] Acciones: suspender, reactivar, eliminar
   - Audit logging en cada acción

7. `app/routes/configuracion/invitaciones.tsx` [NEW]
   - [FEAT] Formulario de invitación por email + selección de rol
   - [FEAT] Invitaciones pendientes con copiar link, reenviar, cancelar
   - [FEAT] Historial de invitaciones con estados (pendiente, aceptada, cancelada, expirada)
   - Validación: duplicados, miembros existentes, expiración 7 días

8. `app/routes/configuracion/roles.tsx` [NEW]
   - [FEAT] Lista de roles expandible con permission matrix
   - [FEAT] Permisos agrupados por categoría
   - [FEAT] Permisos gated por plan (starter, professional, enterprise)
   - [FEAT] Crear rol custom (nombre, descripción, nivel jerárquico)
   - [FEAT] Eliminar rol custom (protección de roles `is_system`)
   - Contador de miembros por rol

9. `app/routes/configuracion/auditoria.tsx` [NEW]
   - [FEAT] Timeline de eventos con avatares y IPs
   - [FEAT] Filtros por acción y tipo de entidad
   - [FEAT] Metadata expandible (JSON pretty-print)
   - [FEAT] Paginación server-side (25 por página)
   - Colores por tipo de acción (create=verde, delete=rojo, etc.)

10. `app/routes/configuracion/organizacion.tsx` [NEW]
    - [FEAT] Info general (slug, plan, estado, fecha creación)
    - [FEAT] Configuración editable: nombre, email facturación, timezone, moneda, color primario
    - [FEAT] Dominios autorizados con auto-asignación de rol
    - Audit logging en cada cambio

11. `app/routes.ts`
    - [CONFIG] Ruta `configuracion` con 5 sub-rutas anidadas

**Deploy:** Version `d744734c`, build exitoso

---

## Sesión 3.1 — 16:45

### [FEAT] RBAC Fase 2: Portal admin.grixi.ai

**Objetivo:** Migrar el panel de administración de plataforma a un subdominio dedicado `admin.grixi.ai` con login y seguridad independientes.

**Archivos modificados:**

1. `workers/app.ts`
   - [SECURITY] Detección de `admin.grixi.ai` como portal admin dedicado
   - `isPlatformAdminPortal` flag en context
   - `tenantSlug = null` cuando es admin portal (aislación completa)
   - Rate limiting aplicado a todo el portal admin
   - [FEAT] Tipo `AppLoadContext` actualizado con `isPlatformAdminPortal`

2. `app/lib/platform-guard.ts`
   - [SECURITY] Soporte para `admin.grixi.ai` (nuevo) + `grixi.grixi.ai` (legacy)
   - `isDedicatedAdminPortal()` para detectar portal dedicado
   - `isPlatformTenant()` acepta ambos métodos

3. `app/routes/authenticated.tsx`
   - [FEAT] `TenantContext` → nuevo campo `isPlatformAdminPortal`
   - [FEAT] Short-circuit para admin portal: valida platform_admin y retorna contexto god-mode
   - No org context necesario en admin portal

4. `app/routes/dashboard.tsx`
   - [SECURITY] Redirect `/dashboard` → `/admin` cuando está en admin portal

**Deploy:** Version `5c39c3c4`, build exitoso. admin.grixi.ai verificado funcionando.

---

### [DB] RBAC Fase 3: PostgreSQL Audit Triggers

**Objetivo:** Implementar auditoría automática a nivel de base de datos. Cada INSERT/UPDATE/DELETE en tablas críticas genera un registro en `audit_logs` con diff de cambios.

**Migración:** `audit_triggers_system`

**Función `fn_audit_trigger()`:**
- Trigger genérico reutilizable para cualquier tabla
- Detección automática de acción (`table.create`, `table.update`, `table.delete`)
- Extracción inteligente de `organization_id` (columna directa o id para orgs)
- Detección de actor: `auth.uid()` → fallback a `invited_by`/`deactivated_by`/`user_id`
- Diff de cambios en UPDATEs (excluye `updated_at`/`created_at` como ruido)
- Snapshot completo del record en INSERT/DELETE
- Metadata enriquecida: `trigger: true`, `table`, `operation`, `schema`

**14 Triggers creados:**

| Tabla | INSERT | UPDATE | DELETE |
|-------|--------|--------|--------|
| `memberships` | ✅ | ✅ | ✅ |
| `roles` | ✅ | ✅ | ✅ |
| `role_permissions` | ✅ | — | ✅ |
| `invitations` | ✅ | ✅ | ✅ |
| `organizations` | ✅ | ✅ | ✅ |

**6 Índices optimizados:** `org_id`, `actor_id`, `action`, `entity(type,id)`, `created_at DESC`, `org+created` (composite)

**Verificación:** Test de update en `organizations` confirmó diff exacto old/new en metadata.

---

### [DB] [SECURITY] Endurecimiento de `platform_admins`

**Migración:** `harden_platform_admins`

- Limpieza de RLS policies duplicadas (`only_admins_see`, `only_admins_manage`)
- Constraint UNIQUE en `user_id` (previene duplicados)
- FK a `auth.users(id)` con `ON DELETE CASCADE`
- Audit triggers (INSERT/UPDATE/DELETE) → integración con `fn_audit_trigger()`
- Índice en `user_id` para lookups rápidos
- Documentación via `COMMENT ON TABLE`

**Estado actual:** 2 admins registrados (`dmera@grixi.ai`, `csaldarriaga@grixi.ai`), sin emails hardcodeados.

---

### [REFACTOR] CSS Lint Cleanup — Tailwind v4

- **Verificación completa:** 0 patrones `[var(--...)]` en className
- **Patrones eliminados:** `border-[var(--border)]` → `border-border`, `text-[var(--text-*)]` → `text-text-*`, `bg-[var(--bg-*)]` → `bg-*`, `bg-gradient-to-r` → `bg-linear-to-r`, `z-[9999]` → `z-9999`
- **Inline styles (`style={{}}`):** Mantenidos correctamente — NO son lints
- Build exitoso + Deploy v `5eac2709`

---

## Sesión 4 — 17:10

### [FEAT] Admin Portal Rebuild: Layout Dedicado + 4 Módulos Nuevos

**Objetivo:** Reconstrucción completa del portal admin.grixi.ai con layout independiente, sidebar dedicado, y 4 módulos nuevos (Plans, Notifications, Settings, Audit avanzado).

**Archivos creados:**

1. `app/components/admin/admin-sidebar.tsx` [NEW]
   - [FEAT] Sidebar dedicado con navegación de 7 secciones
   - [FEAT] Quick stats (orgs, users, 24h) en panel superior
   - [FEAT] Indicador Realtime live/offline con pulse animation
   - [FEAT] Sección de usuario con avatar + logout

2. `app/components/admin/audit-realtime.tsx` [NEW]
   - [FEAT] Hook `useAuditRealtime()` — suscripción Supabase Realtime
   - Postgres Changes INSERT en `audit_logs`
   - Buffer de hasta 50 eventos con pause/resume/clear
   - Estado de conexión (`SUBSCRIBED` detection)

3. `app/routes/admin-layout.tsx` [NEW]
   - [FEAT] Layout completamente independiente del tenant (`authenticated.tsx`)
   - [SECURITY] Guard: `isPlatformTenant()` + `platform_admins` check
   - [FEAT] `AdminCtx` context provider (user, stats, supabase config)
   - [FEAT] Realtime heartbeat channel para sidebar indicator
   - Queries globales para sidebar stats

4. `app/routes/admin/index.tsx` [REWRITE]
   - [FEAT] KPI cards con links + subinfo
   - [FEAT] Crecimiento 30 días (Recharts AreaChart)
   - [FEAT] Distribución de planes (PieChart)
   - [FEAT] Usuarios por org Top 8 (BarChart horizontal)
   - [FEAT] Panel de salud del sistema
   - [FEAT] Actividad reciente con avatares + action badges
   - [FEAT] Acciones rápidas con links directos

5. `app/routes/admin/audit.tsx` [REWRITE]
   - [FEAT] 6 filtros avanzados (acción, org, actor, desde, hasta, búsqueda)
   - [FEAT] Realtime integration con badge NEW + pause/resume
   - [FEAT] Gráfica actividad por hora 24h (BarChart)
   - [FEAT] Timeline expandible con metadata, old/new data diff
   - [FEAT] Paginación server-side + CSV export

6. `app/routes/admin/plans.tsx` [NEW]
   - [FEAT] Cards descriptivos por plan (Demo, Starter, Professional, Enterprise)
   - [FEAT] Tabla de orgs con capacidad visual (progress bars)
   - [FEAT] Cambio de plan con recalculación automática de role_permissions
   - [FEAT] Búsqueda + filtro + CSV export
   - Audit logging completo

7. `app/routes/admin/notifications.tsx` [NEW]
   - [FEAT] Broadcast center: crear, enviar, eliminar notificaciones
   - [FEAT] 4 tipos (info, warning, maintenance, update)
   - [FEAT] Audiencia: all orgs o selección manual
   - [FEAT] Draft/Sent flow + preview
   - Audit logging completo

8. `app/routes/admin/settings.tsx` [NEW]
   - [FEAT] Settings agrupados por categoría (features, limits, system)
   - [FEAT] Toggle para flags booleanos
   - [FEAT] Edición inline para valores numéricos/JSON
   - [FEAT] Catálogo de módulos disponibles
   - [FEAT] Panel de info del sistema
   - Audit logging completo

**Archivos modificados:**

9. `app/routes.ts`
   - [REFACTOR] Admin routes extraídos a `admin-layout.tsx` (layout independiente)
   - Rutas segregadas: admin (8 rutas) vs tenant (authenticated layout)

10. `app/routes/authenticated.tsx`
    - [REFACTOR] Eliminado short-circuit admin portal (27 líneas)
    - Admin portal ahora 100% aislado en su propio layout

**Verificación:**
- TypeScript: ✅ 0 errores
- Build: ✅ 1.05s exitoso

## Sesión 5 — 17:30

### [FEAT] Mejoras críticas al sistema de gestión de miembros e invitaciones

**Archivos afectados:**
- `app/routes/admin/organizations.$id.tsx`

**Cambios:**
- [FEAT] MembersTab — Agregar acciones completas:
    - Cambio de rol inline (select dropdown)
    - Botón "Suspender" con toggle de estado
    - Botón "Eliminar" con confirmación
    - Barra de búsqueda por nombre/email
    - Conteo de miembros activos
- [FEAT] InvitationsTab — Mejoras:
    - Validación de duplicados (usuario ya miembro, invitación pendiente existente)
    - Separación Pendientes vs Historial
    - Botón "Reenviar" (renueva expires_at +7 días)
    - Indicador de expiración visual
    - Display de errores del servidor en banner rojo
    - Botón deshabilitado cuando email vacío
- [FIX] Status mismatch: `cancelled` → `revoked` (alineado con CHECK constraint del schema)
- [FEAT] Nuevos action intents: `resend_invitation`, `change_member_role`, `suspend_member`, `remove_member`
- [SECURITY] Todos los intents loggeados en audit_logs con IP

**Verificación:**
- Build: ✅ 1.01s exitoso
- Deploy: ✅ Cloudflare Workers
- Visual: ✅ Verificado en admin.grixi.ai — formulario de invitación, historial, error display

---

## Sesión 6 — 21:00

### [FIX] Corrección de 12 Bugs — Estabilización Admin Portal

**Objetivo:** Resolver 12 bugs detectados en la auditoría profunda del admin portal, incluyendo 7 de desincronización schema↔código.

**Migración SQL:** `20260330020000_admin_portal_fixes.sql`

| # | Bug | Corrección |
|---|-----|------------|
| 1 | `roles` sin `hierarchy_level` | `ALTER TABLE ADD COLUMN integer DEFAULT 20` + seeds owner=100, admin=80, viewer=10 |
| 2 | `permissions` sin `min_plan` | `ALTER TABLE ADD COLUMN text DEFAULT 'starter'` + seeds ai→professional, admin/reportes→enterprise |
| 3 | `organizations` sin `suspended_at/by` | `ALTER TABLE ADD COLUMN timestamptz/uuid` |
| 4 | `memberships` sin `deactivated_at/by` | `ALTER TABLE ADD COLUMN timestamptz/uuid` |
| 5 | `platform_notifications` no existía | `CREATE TABLE` con RLS + índice + trigger updated_at |
| 6 | `platform_settings` no existía | `CREATE TABLE` con RLS + 13 seeds iniciales |
| 7 | `audit_logs` sin `organization_id` | `ALTER TABLE ADD COLUMN uuid` + índice |
| 9 | Realtime reconexiones por `isPaused` | `useRef` para isPaused, removido del dependency array |
| 11 | `PLAN_COLORS` no utilizado | Eliminado |
| 12 | Paginación limitada a 100 users | Loop con `perPage: 1000` hasta agotar páginas |

**Archivos modificados:**

1. `app/components/admin/audit-realtime.tsx`
   - [FIX] `isPausedRef` + sync effect → evita reconexiones del channel

2. `app/routes/admin/users.tsx`
   - [FIX] Loop paginado `listUsers` con `perPage: 1000`

3. `app/routes/admin/organizations.$id.tsx`
   - [FIX] Loop paginado `listUsers` con `perPage: 1000`
   - [FIX] Eliminado `PLAN_COLORS` sin uso

**Verificación en producción:**
- Settings: ✅ 25 opciones en 3 categorías (features/limits/system)
- Notifications: ✅ Carga correctamente con botón "Nueva Notificación"
- Org Detail: ✅ Miembros con nivel jerárquico visible (100=owner)
- Build: ✅ 1.32s sin errores
- Deploy: ✅ Version `6f1ddacc`

