# Bitácora — 2026-04-22

## Sesión 1 — 21:03

### [CONFIG] Auditoría de Estado Post-Pausa (19 días)
- Última sesión: 2026-04-02
- Verificación completa de infraestructura tras 19 días sin actividad
- **Cloudflare Workers**: ✅ Health OK (303ms latency Supabase)
- **Supabase**: ✅ `ACTIVE_HEALTHY`, PostgreSQL 17.6.1, 21 tablas, 19 migraciones
- **Build local**: ✅ `pnpm build` exitoso (1.02s)
- **Git**: ✅ Limpio, commit `fa10ac8` en sync con origin/main
- **Security Advisors**: 3 warnings menores (search_path, RLS always-true en audit_logs/login_history, leaked password protection)

## Sesión 2 — 22:30

### [PERF] Cloudflare KV Cache — Edge Caching para Authenticated Layout
- **KV Namespace creado**: `7c5b87293fa14664b0187d6831e0bcd8` (binding: `KV_CACHE`)
- **Archivos creados/modificados**:
  - `app/lib/cache/kv.ts` [NEW] — Utilidad centralizada de cache:
    - `getCachedOrFetch()` — wrapper transparente con TTL
    - `invalidateCache()` — elimina keys específicas
    - `invalidateUserCache()` — invalida todo lo relativo a un usuario
    - `invalidateOrgCache()` — invalida config de organización
    - Key builders centralizados: `cacheKey.platformAdmin()`, `cacheKey.userOrgs()`, `cacheKey.userPerms()`
  - `app/routes/authenticated.tsx` — Loader envuelto con `getCachedOrFetch()` para 3 queries:
    - `platform_admin:{userId}` — TTL 10 min (cambia rarísimo)
    - `user_orgs:{userId}` — TTL 5 min (cambia con memberships)
    - `user_perms:{userId}:{orgId}` — TTL 3 min (cambia con roles)
  - `app/routes/configuracion/equipo.tsx` — Invalidación al cambiar roles/suspender/remover miembros
  - `app/routes/configuracion/roles.tsx` — Invalidación al modificar permisos de un rol
  - `app/routes/configuracion/organizacion.tsx` — Invalidación al actualizar settings de org
- **wrangler.jsonc**: KV namespace vinculado automáticamente por wrangler
- **worker-configuration.d.ts**: Tipos regenerados con `wrangler types` → `KV_CACHE: KVNamespace` en `Env`
- **Build**: ✅ exitoso (3.18s)
- **Deploy**: ✅ exitoso — version `97c52263` desplegado a `grixi.ai`
- **Git**: ✅ commit `1b53aeb` pusheado a `origin/main` (cuenta `dmera1`)
- **Decisiones técnicas**:
  - Cache es transparente: si KV no disponible → fallback a queries directos (degradación graceful)
  - Non-blocking puts: `kv.put()` no se awaita para no ralentizar el response
  - Invalidación granular: solo se invalidan keys afectadas, no flush global
  - Impacto estimado: ~300ms ahorrados por request en cache hit (3 queries Supabase eliminadas)
