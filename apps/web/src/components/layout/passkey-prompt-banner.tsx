"use client";

// Passkey infrastructure (Edge Functions + DB table) not yet deployed.
// Banner disabled to prevent broken queries in the demo.
export function PasskeyPromptBanner() {
  return null;
}

